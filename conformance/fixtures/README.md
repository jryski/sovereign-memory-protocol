# Fixtures

No normative machine-readable fixtures have been promoted yet.

Fixture promotion requires stable canonicalization, envelope, result-class, and resource-limit contracts. Generated examples must identify their profile/version and expected byte-level commitments.
